import { Control, TemplateFunction } from 'UI/Base';
import * as template from 'wml!ReactUnitTest/_executor/resources/Generator/AttrKeyRoot';

export default class AttrKeyRoot extends Control {
    _template: TemplateFunction = template;
}
