import { Control, TemplateFunction } from 'UI/Base';
import * as template from 'wml!ReactUnitTest/_executor/resources/Generator/AttrKeyChild';

export default class AttrKeyChild extends Control {
    _template: TemplateFunction = template;
}
