import { Control, TemplateFunction } from 'UI/Base';
import * as template from 'wml!ReactUnitTest/_reactivity/resources/ReactiveControl8Child';

export default class ReactiveControl8Child extends Control {
    _template: TemplateFunction = template;
}
