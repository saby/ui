import { Control, TemplateFunction } from 'UI/Base';
import * as template from 'wml!ReactUnitTest/_reactivity/resources/ReactiveControl7Child';

export default class ReactiveControl7Child extends Control {
    _template: TemplateFunction = template;
}
