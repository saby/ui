/**
 * @author Krylov M.A.
 *
 * Модуль с публичными константами, которые могут быть использованы в шаблонах.
 *
 * Экспортируемые объекты должны быть заморожены!
 */

export const DEFAULT_BREAKPOINTS = Object.freeze({
    sm: 640,
    md: 768,
    lg: 1024
});

export default {
    DEFAULT_BREAKPOINTS
};
